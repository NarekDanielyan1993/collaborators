import { Box, chakra } from '@chakra-ui/react';

export const StyledUsersContainer = chakra(Box, {
    baseStyle: {
        width: '100%',
        maxWidth: '70rem',
        margin: '4rem auto',
    },
});
